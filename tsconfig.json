{
  "compilerOptions": {
    "target": "ES2020", // Specifies the ECMAScript version to compile to. "ES2020" supports modern JavaScript features like optional chaining.
    "module": "commonjs", // Defines the module system. "commonjs" is used in Node.js for compatibility.
    "rootDir": "./src", // Specifies the root directory of your TypeScript files. All files should reside under "src".
    "outDir": "./dist", // Specifies the directory where the compiled JavaScript files will be output.
    "strict": true, // Enables all strict type-checking options, improving code quality and reducing potential errors.
    "esModuleInterop": true, // Enables compatibility between CommonJS and ES Module imports, allowing `import` statements for CommonJS modules.
    "skipLibCheck": true, // Skips type checking of declaration files (*.d.ts) for faster compilation.
    "forceConsistentCasingInFileNames": true, // Ensures file imports match the exact casing of the file name to avoid issues on case-sensitive systems.
    "baseUrl": "./src", // Sets the base directory for resolving non-relative module imports to the "src" folder.
    "paths": { 
      "@controllers/*": ["controllers/*"], // Alias for "src/controllers" to simplify imports.
      "@services/*": ["services/*"], // Alias for "src/services" to simplify imports.
      "@utils/*": ["utils/*"] // Alias for "src/utils" to simplify imports.
    }
  },
  "include": [
    "src/**/*" // Includes all TypeScript files in the "src" folder and its subfolders.
  ],
  "exclude": [
    "node_modules", // Excludes dependencies from type checking and compilation.
    "dist" // Excludes the compiled output to prevent circular processing.
  ]
}
